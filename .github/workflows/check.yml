name: Check

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: check-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    container: archlinux:latest
    env:
      OMARCHY_PATH: ${{ github.workspace }}/.ci/omarchy
    steps:
      - name: Install tools
        run: |
          pacman -Syu --noconfirm --needed \
            base-devel \
            git \
            namcap \
            nodejs \
            python \
            python-pytest \
            qt6-declarative \
            qt6-multimedia \
            shellcheck
      - uses: actions/checkout@v7
      - uses: actions/checkout@v7
        with:
          repository: basecamp/omarchy
          ref: a466dcc04f937a41c820aaa990a31f36ecaed543
          path: .ci/omarchy
      - name: Trust container-mounted checkouts
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global --add safe.directory "$OMARCHY_PATH"
      - name: Configure Qt tools
        run: echo "/usr/lib/qt6/bin" >> "$GITHUB_PATH"
      - name: Verify required runtimes
        run: |
          python3 --version
          node --version
      - name: Run checks
        run: ./scripts/check.sh
      - name: Rehearse AUR package
        run: scripts/rehearse-aur-package
